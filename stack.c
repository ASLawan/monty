#include "monty.h"
/**
 * stack - sets data structure to stack
 * @stack: pointer to stack
 * @line_number: instruction
 * Return: nothing
 */

void stack(stack_t **stack, unsigned int line_number)
{
	(void)stack;
	(void)line_number;

	input->data = 1;
}
