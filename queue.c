#include "monty.h"
/**
 * queue - sets data structure to queue
 * @stack: pointer to stack
 * @line_number: instruction
 * Return: nothing
 */

void queue(stack_t **stack, unsigned int line_number)
{
	(void)stack;
	(void)line_number;

	input->data = 0;
}
